api-gateway:
  build:
    context: ./ApiGateway
  container_name: api-gateway
    depends_on:
      - billingserviceapi
      - bookserviceapi
      - orderserviceapi
      - userserviceapi
    environment:
      ASPNETCORE_URLS: "http://+:8000"
    pull_policy: always